# Polymorph backend

A backend for polymorph.


## Usage
1. Copy `private_sample.js` to `private.js`
3. Run on your server: `node index.js <port> <privateFileName>` - both parameters optional.
    - Default private file name is private
    - Default port is 8080.